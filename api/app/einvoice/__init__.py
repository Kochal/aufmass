# api/app/einvoice/__init__.py
# E-invoice generation and validation package (directive 06, EN 16931).
# All money math is done by the pricing engine before this package is called.
# This package only assembles and validates the XML representation.
